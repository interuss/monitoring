{
  "$id": "https://github.com/interuss/monitoring/blob/main/schemas/monitoring/uss_qualifier/resources/interuss/uss_identification/AccessTokenIdentifier.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "monitoring.uss_qualifier.resources.interuss.uss_identification.AccessTokenIdentifier, as defined in monitoring/uss_qualifier/resources/interuss/uss_identification.py",
  "properties": {
    "$ref": {
      "description": "Path to content that replaces the $ref",
      "type": "string"
    },
    "issuer": {
      "description": "If specified, this identifier only applies to access tokens from this issuer.  If not specified, this identifier applies to any access token.",
      "type": [
        "string",
        "null"
      ]
    },
    "subject": {
      "description": "If specified, assume the participant is responsible for applicable access tokens containing this subject.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "type": "object"
}