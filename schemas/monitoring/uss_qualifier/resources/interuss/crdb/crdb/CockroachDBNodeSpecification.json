{
  "$id": "https://github.com/interuss/monitoring/blob/main/schemas/monitoring/uss_qualifier/resources/interuss/crdb/crdb/CockroachDBNodeSpecification.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "monitoring.uss_qualifier.resources.interuss.crdb.crdb.CockroachDBNodeSpecification, as defined in monitoring/uss_qualifier/resources/interuss/crdb/crdb.py",
  "properties": {
    "$ref": {
      "description": "Path to content that replaces the $ref",
      "type": "string"
    },
    "host": {
      "description": "Host where the CockroachDB node is reachable.",
      "type": "string"
    },
    "participant_id": {
      "description": "ID of the USS responsible for this CockroachDB node",
      "type": "string"
    },
    "port": {
      "description": "Port to which CockroachDB node is listening to.",
      "type": "integer"
    }
  },
  "required": [
    "host",
    "participant_id",
    "port"
  ],
  "type": "object"
}